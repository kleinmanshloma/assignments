<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
    />
    <title>Hebrew Date Converter</title>
  </head>
  <body>
    <div class="container">
      <h1 class="row mt-5 col justify-content-md-center">
        Hebrew Date Converter
      </h1>
      <h2 class="row mt-5 mb-4 col justify-content-md-center">Today's Date</h2>

      <p
        style="font-size: 1.5rem; font-weight: 200"
        id="today's-date"
        class="row col justify-content-md-center"
      >
        <%= todaysDate %>
      </p>
      <p
        style="font-size: 1.5rem; font-weight: 200"
        id="today's-date"
        class="row col justify-content-md-center"
      >
        <%= hebrewDate %>
      </p>
      <p
        style="font-size: 1.5rem; font-weight: 200"
        id="today's-date-hebrew"
        class="row col justify-content-md-center mb-5"
      ></p>
      <form class="row justify-content-md-center mt-3">
        <div class="mb-3 col-3">
          <label class="form-label" for="date">Date</label>
          <input class="form-control" type="date" name="date" id="date" />

          <p
            style="font-size: 1.5rem; font-weight: 200"
            id="today's-date"
            class="row col justify-content-md-center"
          >
            <% if(searchDate) { %> <%= searchDate %> <% } %>
          </p>

          <button id="btn-convert" class="btn btn-info mt-3" type="submit">
            Convert to Hebrew
          </button>
        </div>
      </form>
    </div>

    <script src="../publick/hebrew-date-converter.js"></script>
  </body>
</html>
